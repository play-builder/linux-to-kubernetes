# pending-test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: pending-test
spec:
  nodeSelector:
    disktype: nonexistent      # 어느 노드에도 없는 라벨 → 배치 가능 노드 0 → Pending 재현
  containers:
  - name: nginx
    image: nginx
