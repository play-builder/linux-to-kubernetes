# 07_03_istio_ambient_zero_trust/04-security/optional/mesh-wide-strict.yaml
# 참고용 — 카나리 사다리(0~4단계) 완주 후에만 적용. 폴더 일괄 적용 금지.
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system    # 루트 네임스페이스 = mesh 전체 기본값
spec:
  mtls:
    mode: STRICT
